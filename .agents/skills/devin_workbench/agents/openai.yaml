interface:
  display_name: "Devin Workbench"
  short_description: "Repo-specific patterns and workflows for devin_workbench"
  default_prompt: "Use the devin_workbench repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true